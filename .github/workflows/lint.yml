name: Lint
on: push
jobs: 
  lint:
    name: Lint the code
    runs-on: 
    steps: 
      - uses: actions/checkout@v2
      - name: Setup Deno
        uses: denolib/setup-deno@v2
        with: deno-version: "v1.x"
      - run: |
        deno --version
        deno test ./test